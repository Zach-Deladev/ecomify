import React from 'react'

// This component should initially show all products unless a specifix category has been chosen, if a category has been selected then this should conditionally render the grid of products with only those items. 
const Products = () => {
  return (

    <>
    
    {/* add conditionally rendered grid */}
    </>
   
  )
}

export default Products